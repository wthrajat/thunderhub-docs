import { Callout, Tabs } from 'nextra/components';

# Installation

## Requirements

- **Node.js** version 18 or higher
- **npm**

## From Source

Clone the repository and build:

```bash copy
git clone https://github.com/apotdevin/thunderhub.git
cd thunderhub
npm install
npm run build
npm start
```

The application starts on port `3000` by default. Open `http://localhost:3000` in your browser.

To use a different port, set the `PORT` environment variable:

```bash copy
PORT=4000 npm start
```

For production deployments, you can reduce disk usage by pruning development dependencies after the build:

```bash copy
npm prune --production
```

## Updating

Run all commands from inside the `thunderhub` directory.

<Tabs items={['Script', 'Manual']}>
  <Tabs.Tab>```bash copy npm run update ```</Tabs.Tab>
  <Tabs.Tab>
    ```bash copy git pull npm install npm run build npm start ```
  </Tabs.Tab>
</Tabs>

## Docker

Docker images are published to [Docker Hub](https://hub.docker.com/repository/docker/apotdevin/thunderhub).

```bash copy
docker pull apotdevin/thunderhub:latest
docker run --rm -it -p 3000:3000 apotdevin/thunderhub:latest
```

Open `http://localhost:3000` to access ThunderHub.

<Callout type="info">
  See the [Configuration](/setup) page for details on connecting ThunderHub to
  your Lightning node via environment variables or a YAML config file.
</Callout>

## Development

To run ThunderHub in development mode with hot reloading:

```bash copy
npm run dev
```
